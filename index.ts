// nomeVariavel : TipoVariavel



function enviarEmail(nome: string, email: string,) {
    console.log('Olá ${nome} seu email é ${email}')
}

enviarEmail(
     "IsaacNascimento@email.com",
     "Isaac"

)